# Tutorial - Fuzzing OPC-UA server

1) there are files for server and client
2) there are modifications to be done on the server and client
3) the server and client can be instrumented and built
4) we should create initial seeds for fuzzing 
5) we start the fuzzer which talks to the server.
